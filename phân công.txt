Đặng Nguyễn Minh Luân:  + Tạo database + setup structure
                        + POST trang đăng ký
                        + POST trang đăng nhập
                        + GET danh sách ảnh về
                        + GET tìm kiếm danh sách ảnh theo tên

Lương Sỹ Bảo:           + GET thông tin ảnh và người tạo ảnh bằng id ảnh
                        + GET thông tin bình luận theo id ảnh
                        + GET thông tin đã lưu hình này chưa theo id ảnh
                        + POST để lưu thông tin bình luận của người dùng với hình ảnh
                        + GET thông tin user

Hoàng Hải:              + GET danh sách ảnh đã lưu theo user id
                        + GET danh sách ảnh đã tạo theo user id
                        + DELETE xóa ảnh đã tạo theo id ảnh
                        + POST thêm một ảnh của user
                        + PUT thông tin cá nhân của user